# 2026-03-09

- 用户新增硬规则（Discord #gpr-gui 线程）：后续任务只要“像之前一样使用 subagent manager skill”对任务是正向收益，就必须使用；作为强制执行要求。
- 执行约束：默认优先评估是否可由 subagent_manager 承接；若可提升效率/质量/并行能力，则必须走该技能。
- 例外：仅在明显不适配（极小单步回复、无需工具、即时澄清）时可不启用，并需在回复中简短说明原因。
- 用户新增发布规则（Discord #gpr-gui-performance-opt）：今后上传 EXE 必须携带 release 版本号信息（文件名或发布元数据中可追踪）。
