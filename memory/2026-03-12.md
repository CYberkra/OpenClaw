- User provided three legacy Python scripts (FDTD/tools utilities, imagesc + Kir/RTM processing, RTM GPU) for CaGPR migration pipeline and asked if they suffice; acknowledged these are core modules; started integration task to package into GPR_GUI legacy mode.
