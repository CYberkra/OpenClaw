# 2026-03-20

## Durable updates
- PinchTab Windows long-term strategy chosen: adopt a hybrid "master-slave" approach (existing OpenClaw browser tool as primary, PinchTab as secondary/specialized path).
- PinchTab was installed on Windows and manually started to `server started`, but default instance repeatedly stayed in `starting/error` and returned `instance not ready (may be restarting)` for navigate tests.
- Confirmed Google Chrome is installed at `C:\Program Files\Google\Chrome\Application\chrome.exe`; likely issue is PinchTab instance/runtime initialization instability rather than missing Chrome alone.
- Model outage diagnosis: failures were caused by routing into providers without configured keys (observed missing auth for `kimi-coding`, `anthropic`, and non-OAuth `openai` provider path), while main session remained healthy on `openai-codex/gpt-5.3-codex` via OAuth.
- Stabilization direction agreed: prefer minimal-risk fixes by locking to available model/auth path first, then optionally re-enable other providers only after key configuration.
