## 规则归档
- 时间：2026-03-10 15:27 GMT+8
- 事项：新增“GUI 迭代提交规范（主线程+子线程统一）”并归档生效。
- 摘要：
  1) 每次 GUI 迭代提交必须使用固定模板：`type(scope): summary`。
  2) 提交前最小检查：`git status`、关键功能自检、变更文件清单确认。
  3) 提交后必须产出简报：变更点 / 风险 / 回滚点 / 产物路径。
  4) 禁止提交环境噪音文件：cache/log/dist 中间产物等。
  5) 适用范围统一：main session + all subagents。

## AI办公室/可视化落地进展（Star-Office-UI）
- 时间：2026-03-10 20:00-22:35 GMT+8
- 背景：用户要求在“能复用就复用”的前提下，快速实现类似“AI 办公室/多 Agent 可视化在岗状态”的效果，不从零自研。

### 关键决策
1) 方案选型：优先采用 `ringhyacinth/Star-Office-UI` 作为可视化底座，先跑通功能再做视觉替换。
2) 实施策略：
   - 先做状态链路闭环（事件驱动优先 + 轮询兜底）
   - 后做素材替换（地图/角色分阶段）
3) 用户偏好确认：当前阶段先保留默认素材，后续再慢慢调视觉。

### 已完成成果（可复用）
1) 最小落地包已生成：
   - `research/star-office-ui-plan/QUICKSTART.md`
   - `research/star-office-ui-plan/.env.example`
   - `research/star-office-ui-plan/state-mapping.md`
   - `research/star-office-ui-plan/runbook.md`
2) 冒烟脚本与自动化脚本：
   - `scripts/star-office-smoke.sh`
   - `scripts/star-office-state.sh`
   - `scripts/star-office-watch-subagents.sh`
   - `scripts/star-office-event.sh`
   - `scripts/spawn-with-star-office.sh`
3) service 常驻成功：
   - `~/.config/systemd/user/star-office-watch.service` 已运行稳定
4) P0 兼容修复完成：
   - 状态映射：`coding->executing`、`debugging->researching`、`reviewing->writing`、`idle->idle`
   - `/set_state` payload 双字段兼容：同时发送 `text` 与 `detail`

### 关键问题与解决
1) 轮询误判：仅靠 sessions 轮询无法稳定反映实时 subagent 活跃，导致看板常驻 idle。
   - 解决：改为事件驱动打点（task_start/task_done/subagent_spawn/subagent_done），轮询仅兜底。
2) 多 agent 接入失败：
   - 原因A：错误使用不存在的 `office-agent-join.py`；实际应使用 `office-agent-push.py`
   - 原因B：`JOIN_KEY` 不匹配（本地 `join-keys.json` 实际可用 key 为 `ocj_example_team_01`）
   - 原因C：`office-agent-push.py` 默认读取脚本内常量，不读取 shell export（需改文件头）

### 产出文档（调研/评估）
- `reports/star_office_resource_catalog.md`
- `reports/star_office_integration_plan_v2.md`
- `reports/star_office_ui_fit_assessment.md`（结论：部分适合，匹配度 78/100）
- `research/star-office-ui-plan/event-driven-runbook.md`

### 当前状态（截至 22:35）
- 单 agent 运行与状态切换已稳定。
- 多 agent 已开始接入；第一个成功加入，第二个因脚本头配置未填需修复后可加入。
- 下一步优先级：完成多 agent 全接入与3轮稳定性验证，再进入素材替换阶段。
