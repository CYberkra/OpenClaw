# 2026-03-17

- 固化默认路由判定表 v1：纯问答主进程直答；代码改动默认 opencode；多步骤/多工具任务必走 subagent_manager；调研走 researcher；高风险/不稳定结果追加 reviewer；适合自动验收的项目任务启用 CI。
- 固化验收闸门 SOP v1：执行完成 → 执行者自检 → 主进程人工核验 → CI/自动验收（适用即强制）→ reviewer 复核（命中时强制）→ 对外回传。任一闸门失败不得宣称完成。
- 增量优化工作流规则 v1.1（低风险快改）：补齐角色边界、模式切换、规则优先级、防漂移与最小关键词集。
- 落地 3 条防御性补丁 v1.2：单节点自检/自动修复循环最多 3 次；外部依赖长时间无输出则超时熔断并视为闸门失败；日常闲聊/简单路由只常驻骨架规则 + rules/INDEX.md，复杂路由再按需加载。
- 新增 Discord 交互偏好 v1.3：在 Discord 中，凡涉及授权、选择、继续/暂停、验收级别、模式切换，默认优先使用可点击交互，减少用户打字；高风险动作必须明确动作内容。
- 新增 SYSTEM_BOOTSTRAP 维护策略：凡默认工作流拓扑、路由/验收/模式系统、规则入口与优先级、关键常量发生稳定变化时，必须事件驱动更新 `SYSTEM_BOOTSTRAP.md`；此外每 7–14 天做一次轻量一致性检查。
- 调整“自我规则定期备份与巡检”频率：由每 10 天一次改为每 3 天一次，并同步到 HEARTBEAT 与规则方案文档。
- 固化工作流模式持久化策略：所有模式默认"仅限单条任务一次性"，任务完成后自动回退到 default 模式；仅在用户明确指令并再次确认后，才允许长期有效（且高风险模式每次任务前仍需确认）。
- 落地智能交互提示方案 v1.4：在6类关键决策点（复杂任务起始、高影响动作前、工作流模式选择、任务受阻时、验收决策点、定期巡检）自动弹出可点击按钮，内容先于按钮呈现，15分钟超时机制，打字回复可覆盖。

---

## 架构升级记录：YAML Frontmatter + 按需加载机制

**升级原因**：解决 Token 爆炸和上下文丢失问题。通过标准化的 YAML Frontmatter 元数据，使主进程能够快速识别文档类型、优先级和适用范围，实现真正的按需加载。

**升级时间**：2026-03-17 13:20

**变更文件清单**：

| 文件路径 | id | type | priority |
|---|---|---|---|
| `skills/user-preferences/SKILL.md` | rule-user-preferences-main | core-system | high |
| `AGENTS.md` | rule-agents-overview | core-system | high |
| `rules/INDEX.md` | rule-index-registry | core-system | high |
| `skills/rule-archive-lite/SKILL.md` | rule-archive-lite-snapshot | core-system | medium |
| `rules/self-backup-audit-v1.md` | rule-self-backup-audit | skill-detail | medium |
| `SYSTEM_BOOTSTRAP.md` | rule-system-bootstrap | core-system | medium |
| `USER.md` | rule-user-profile | agent-profile | medium |
| `TOOLS.md` | rule-tools-mappings | tool-spec | medium |

**INDEX.md 结构变化**：
- 从「轻量总索引」升级为「Frontmatter 注册表」
- 新增标准 Frontmatter 字段表（id, type, priority, scope, description）
- 保留规则优先级、文档路由速查、变更联动原则、用户关键词速查

**按需加载机制**：
- 主进程默认只读取 `rules/INDEX.md` 和各文件的 Frontmatter
- 命中 scope 关键词或匹配 description 意图时，才加载全文
- 禁止每轮预加载所有长文规则文件

**潜在风险**：
1. 旧版非标准 Frontmatter 被替换，若外部有解析依赖可能受影响（但旧格式本身也不标准）
2. 需要确保所有新规则文档都遵循 Frontmatter 标准
3. 子代理需要理解新的按需加载策略

**验证状态**：所有核心文档已添加标准 Frontmatter，INDEX 已重写为注册表格式。

**Commit 信息**：
- Hash: `3f638f2e33d05c08f74a9ad1801e2cf3a4d45735`
- Push 状态: 被 GitHub Push Protection 阻止（历史 commits 中有 secrets，非本次变更）
- 解决建议: 需清理历史 commits 中的 secrets 或使用 `git push --force-with-lease`（谨慎）
