# 2026-03-17

- 固化默认路由判定表 v1：纯问答主进程直答；代码改动默认 opencode；多步骤/多工具任务必走 subagent_manager；调研走 researcher；高风险/不稳定结果追加 reviewer；适合自动验收的项目任务启用 CI。
- 固化验收闸门 SOP v1：执行完成 → 执行者自检 → 主进程人工核验 → CI/自动验收（适用即强制）→ reviewer 复核（命中时强制）→ 对外回传。任一闸门失败不得宣称完成。
- 增量优化工作流规则 v1.1（低风险快改）：
  - 补齐角色边界：manager 仅编排；opencode 默认执行代码；coder 作为降级兜底。
  - 补齐模式切换：default/strict/proactive/evolver/ralph 的触发关键词与冲突优先级（strict > ralph > evolver > proactive > default）。
  - 补齐规则优先级与防漂移：明确 user-preferences 为执行主源（SSOT），并要求硬规则变更同轮联动更新 user-preferences + rule-archive-lite + 当日 memory。
  - 补齐用户可调用关键词：路由/模式/验收三类最小口令集。
- 落地 3 条防御性补丁 v1.2：单节点自检/自动修复循环最多 3 次；外部依赖长时间无输出则超时熔断并视为闸门失败；日常闲聊/简单路由只常驻骨架规则 + rules/INDEX.md，复杂路由再按需加载。
- 新增 Discord 交互偏好 v1.3：在 Discord 中，凡涉及授权、选择、继续/暂停、验收级别、模式切换，默认优先使用可点击按钮/组件，减少用户打字；高风险动作必须明确动作内容，不适合按钮时才退回文字确认。
