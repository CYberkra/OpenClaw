# 2026-03-16

- 归档用户新规则：如果 WSL 中已接入并可用 opencode，后续代码类任务默认优先由我调用 opencode 执行；我负责分派、验收、汇总与回传。若 opencode 不可用、失败或明显不适配，再切回其他路径并说明原因。
- 归档用户新规则：凡是调用 opencode、子进程或其他执行器完成代码/UI/截图类任务，不能直接转发结果，必须先由主进程做核验；涉及界面或中文显示时，必须亲自查看截图或等效可见证据后再回复。
