# 2026-03-16

- 归档用户新规则：如果 WSL 中已接入并可用 opencode，后续代码类任务默认优先由我调用 opencode 执行；我负责分派、验收、汇总与回传。若 opencode 不可用、失败或明显不适配，再切回其他路径并说明原因。
- 归档用户新规则：凡是调用 opencode、子进程或其他执行器完成代码/UI/截图类任务，不能直接转发结果，必须先由主进程做核验；涉及界面或中文显示时，必须亲自查看截图或等效可见证据后再回复。
- 归档用户新规则：GitHub Actions/CI 不只用于 GUI；后续只要项目存在自动化验收、回归检测、性能/质量门禁、构建测试或产物归档的正向收益，就应优先评估并可直接采用 CI/Actions 方案优化流程。
- 固化规则分流准则 v1：临时上下文→daily memory；用户长期执行偏好→user-preferences；规则历史/快照→rule-archive-lite；用户画像→USER.md；环境映射→TOOLS.md；项目规则→项目目录；工作区高层规则→AGENTS.md；若规则同时影响执行与追溯，则联动更新主生效文件+归档文件+当日 memory。
