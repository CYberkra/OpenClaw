[09:40] 消融指标默认数值（A8-NEW-1）

Context / 数据：

dataset: A8-NEW-1.csv（第4列幅值，801×79）

repo: /home/baiiy1/.openclaw/workspace/repos/GPR_GUI + temp_repository

script: compare_bg_agc.py + 指标计算脚本（临时）

params: target窗口=40×10；搜索范围y=100~700，步长(10,5)；noise窗口为RMS最小且不与target重叠；SNR=RMS比(dB)；SSIM为min-max归一化全局SSIM；耗时为单次运行(WSL)测量

Steps / 做了什么：

- 基于baseline_subavg/bg_svd/agcimp_subavg 的CSV输出计算SNR/对比度/SSIM
- 在PPTX中更新消融指标表与结论文本
- 将更新后的PPTX拷贝到 temp_repository 并提交

Results / 结果：

图像/输出：/home/baiiy1/.openclaw/workspace/reports/探地雷达_背景抑制与AGC改进_汇报_2026-03-02.pptx

指标：Baseline SNR=11.97 dB, 对比度=4.26, SSIM=1.000, 耗时=1.16 s；SVD SNR=11.64 dB, 对比度=4.04, SSIM=0.995, 耗时=0.65 s；AGC改进 SNR=8.33 dB, 对比度=2.65, SSIM=0.210, 耗时=0.71 s

结论：SVD背景抑制与基线接近；当前AGC改进参数下SNR/对比度下降，需要调参验证。

Issues / 风险：

- 指标依赖目标/噪声窗口定义与搜索范围；A8-NEW-1 含 NaN trace 需进一步处理

Next / 下一步：

researcher: —

coder: 继续补充不同窗/秩/无AGC消融

reviewer: 复核窗口定义与SSIM口径
