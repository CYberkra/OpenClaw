# 2026-03-08

## Durable updates
- Lab auth troubleshooting: `auth-profiles.json` existed but Anthropic key availability/path consistency caused repeated "no API key for anthropic" behavior. Practical fix used: copy main agent auth profile into lab agent dir (`/home/baiiy1/labhome/.openclaw/agents/main/agent/auth-profiles.json`) and restart lab gateway on `18891`.
- Lab instability root cause observed repeatedly: duplicate gateway instance on port `18891` (already-running process conflict), not only auth.
- User decided to postpone Lab reliability hardening for now ("今天先不折腾这个lab了，推迟一下").
- HEARTBEAT updated and pushed: include task queue summary + queued tasks details in periodic report. Commit: `fd160d0`.
- Discord components v2/button capability was tested successfully (button message sent and clickable).
- User preference clarified: do **not** keep fixed long-term decision buttons by default; only use buttons when explicitly needed.
- FIS trial initiated: local lifecycle demo run (create/list/complete) with artifacts under `/mnt/e/Openclaw/.openclaw/workspace/shared/fis_eval/`.
- One-shot cron created for FIS evaluation report at `2026-03-08 08:00 +08:00` to current Discord channel. Cron id: `c6f0a5d5-9a0f-4084-9f9b-ca49ea1a3a82`.
- Current isolation-zone assessment: development exists but branch/worktree is messy; recommendation is single-feature convergence (speed/depth conversion), produce clean proposal package (purpose + diff/stat + risk + rollback) before any mainline merge.
- User requested future capability: learn and provide robust GPR post-processing quality evaluation (how to judge good/bad from images + metrics).

[13:28] GPR隔离区流程优化与评判完成

Context / 数据：

dataset: /mnt/e/Openclaw/.openclaw/workspace/isolated/GPR_GUI_evolve/local_data/测线main_40dbm_普通飞高.csv

repo: isolated/GPR_GUI_evolve (隔离实验分支态，未并入主线)

script: tools/optimize_and_evaluate.py

params: SEC(gain_min=1.0,gain_max=5.5,power=1.15); SVD(rank=1); FK(10-62,taper=4); Hankel(window=64,rank=8); sliding_avg(window=11,axis=1)

Steps / 做了什么：

1) 基线化现有处理链并记录参数与输出。
2) 优化Hankel对角平均与深度轴换算（向量化），保持结果一致性。
3) 计算SNR/CNR/背景残差/结构相关/锐度并输出对比图与CSV。

Results / 结果：

图像/输出：isolated/GPR_GUI_evolve/output/comparison_before_after.png

指标：runtime 236.69s->57.42s(4.12x); depth_convert 223.54ms->7.00ms(31.9x); SNR/CNR/背景残差/结构相关/锐度保持不变

结论：本轮优化显著提速且质量不退化，建议并入主线实现。

Issues / 风险：

输入矩阵形状为(1430355,5)，疑似原始CSV为“样本数>>道数”结构；后续可增加自动转置/方向识别策略。

Next / 下一步：

researcher: 增加多数据集鲁棒性验证与方向自动判别。

coder: 将向量化Hankel与深度换算补丁迁移到主线处理模块。

reviewer: 复核多场景质量一致性并决定并入窗口。
